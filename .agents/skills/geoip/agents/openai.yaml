interface:
  display_name: "Geoip"
  short_description: "Repo-specific patterns and workflows for geoip"
  default_prompt: "Use the geoip repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true